# Stretch goals

- **Peak-penalized objective**: Sweep λ in min(… + λ max_t ∑_i p_i(t)); report cost–peak tradeoff.
- **Interactive what-if**: Charger outage, demand surge; modify constraints, re-solve, compare.
