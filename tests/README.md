# Tests

- **Data**: loader, standardized format conversion.
- **Constraints**: checker on synthetic schedules (valid/invalid).
- **Metrics**: cost, peak, unmet, violations from known schedule.
- **Integration** (optional): baseline and agent pipelines on small fixtures.
