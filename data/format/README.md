# Standardized session format

- Per-session fields: arrival time `a_i`, departure time `d_i`, requested energy `E_i` (kWh), assigned charger, per-session max power/current.
- Used by baseline, agent, and constraint checker.
