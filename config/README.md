# Config

- **Site constraints**: `P_max(t)`, per-charger limits, time step ∆ (e.g. 5 or 15 min).
- **TOU rates**: `c(t)` vector per experiment (fixed per run; §5).
- **Experiments**: 5+ days with varying congestion; reference to ACN-Data site/date filters.
